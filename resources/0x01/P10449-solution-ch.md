# 题解：P10449 费解的开关

> 源代码：`P10449.cpp`

---

## Hint 1

<details>
<summary>Hint 1 问题</summary>

对一个灯泡做操作相当于对相邻四个灯泡和这个灯泡做什么操作？从中能得出什么性质？

</details>

<details>
<summary>Hint 1 答案</summary>

对一个灯泡做操作，相当于把这个灯泡以及其**上下左右**四个邻居的状态**取反**（0 变 1，1 变 0）。

这个操作是**可逆的**：连续对同一个灯泡做两次操作等于什么都不做。

因此，每个灯泡只有「按」与「不按」两种有效选择，并且按的顺序不影响最终结果。

</details>

---

## Hint 2

<details>
<summary>Hint 2 问题</summary>

一个灯泡会被操作两次吗？为什么？

</details>

<details>
<summary>Hint 2 答案</summary>

不会。

因为同一个灯泡按两次等于没按，按三次有两次无效，不如只按一次。我们只需关心每个灯泡最终按了**奇数次**还是**偶数次**，即按与不按。

</details>

---

## Hint 3

<details>
<summary>Hint 3 问题</summary>

灯泡之间的操作顺序交换会影响结果吗？为什么？

</details>

<details>
<summary>Hint 3 答案</summary>

不会。

无论你先按哪个后按哪个，每个灯泡最终被翻转的次数只取决于它和它的邻居被按的次数的奇偶性。

翻转操作本身是**交换律和结合律**都满足的异或运算（`^1`），所以顺序无关。

</details>

---

## Hint 4

<details>
<summary>Hint 4 问题</summary>

你是否得出了一个基础的解法？

</details>

<details>
<summary>Hint 4 答案</summary>

一个朴素的搜索思路是：

1. 枚举每一个灯泡是否被按（2^25 种状态），检查能否在 6 步内全亮。
2. 或者只枚举所有可能的 6 步组合（但步数限制不一定严格 6，可能是任意 ≤6 的步数）。

这种直接暴搜在 25 盏灯的情况下搜索量过大，需要进一步优化。

</details>

---

## Hint 5

<details>
<summary>Hint 5 问题</summary>

这个解法能被优化吗？想一下，我们从上往下操作，上一行的灯泡状态确定了（哪个开，哪个关），那么当前行的状态也能被确定吗？为什么？

</details>

<details>
<summary>Hint 5 答案</summary>

可以！

固定第一行按法后，从第二行开始，**每一行的按法唯一确定**：为了让上一行的某个灯泡变亮，必须按下它正下方的灯泡（因为上面和左右的已经处理过了）。

具体来说：处理完前 i 行后，第 i-1 行的所有灯泡必须都是亮着的。如果第 i-1 行第 j 列是灭的，则必须按 (i, j)，让它和它上方的灯泡一起翻转。

这样只要第一行固定了，整个 5×5 矩阵的按法就**唯一确定**。

</details>

---

## Hint 6

<details>
<summary>Hint 6 问题</summary>

那我们的最终的解法会是怎么样的？

</details>

<details>
<summary>Hint 6 答案</summary>

1. 枚举第一行的所有按法（共 2^5 = 32 种）。
2. 对于每种第一行按法，逐行向下模拟：若上一行某列为 0，则按当前行对应列的开关。
3. 最后检查最后一行是否全部为 1。如果全亮，记录按的步数。
4. 在所有可行的第一行方案中取最小步数，若最小步数 ≤6 则输出，否则输出 -1。

复杂度：O(2^5 × 5 × 5)，完全可行。

</details>

---

## Solution

<details>
<summary>查看完整思路与代码解析</summary>

### 核心思想

本题的关键观察是：一旦第一行的按法确定，**剩余所有行的按法就唯一确定了**。因为要让第 i-1 行全亮，第 i 行的按法必须恰好修正第 i-1 行中所有灭着的灯——按它正下方的位置。最后只需检查最后一行是否全亮即可验证该方案是否可行。

我们只需枚举第一行的 2^5 = 32 种按法，模拟每一种，取最小步数。

### 1. 数据结构与方向数组

```cpp
int a[5][5], b[5][5];       // a 存初始状态，b 是用于模拟的副本
int fx[] = {0, 1, 0, -1};
int fy[] = {1, 0, -1, 0};
```

`apply(x, y)` 将 `(x, y)` 本身与四个方向的灯状态取反（0↔1）。方向数组配合 for 循环遍历上下左右四个邻居，加上 (x, y) 自身，共翻转 5 个位置。注意需要判断边界，不能越界访问数组。

### 2. 枚举第一行

```cpp
for (int i = 0; i < (1 << 5); ++i) {
    int cnt = 0;
    memcpy(b, a, sizeof(a));  // 复制初始状态
    for (int j = 0; j < 5; ++j)
        if (i >> j & 1)       // 第一行第 j 列需要按
            apply(0, j), ++cnt;
```

变量 `i` 从 0 到 31，其二进制表示第一行的按法：第 j 位为 1 表示按第一行第 j 列。`memcpy` 将初始状态复制到 `b` 中用于模拟，确保每种第一行方案都在原始棋盘上独立运行。

### 3. 逐行确定（核心递推）

```cpp
for (int j = 1; j < 5; ++j) {
    for (int k = 0; k < 5; ++k) {
        if (b[j-1][k] == 0)   // 上一行未亮
            apply(j, k), ++cnt;
    }
}
```

处理第 1 行到第 4 行。对于当前行 j，检查上一行 j-1 的每一列：如果 `b[j-1][k] == 0`（灭），则必须按 `(j, k)` 来点亮它。为什么按正下方？因为 (j-1, k) 的上方和左右邻居都已经处理过了（要么被第一行固定，要么被更上一行修正），唯一还能影响它的就是正下方的位置。

这一步是全算法的精髓——用「上一行约束当前行」把指数级搜索降为线性模拟。

### 4. 验证最后一行

```cpp
bool flag = true;
for (int j = 0; j < 5; ++j)
    for (int k = 0; k < 5; ++k)
        flag &= b[j][k];
if (flag) ans = min(ans, cnt);
```

经过逐行修正后，前 4 行一定全亮（这是我们在第 3 步中保证的），但最后一行没有"下一行"来修正它。因此最后一行必须自身全亮，方案才算有效。如果全亮，用 `ans = min(ans, cnt)` 更新最小步数。

### 5. 输出

```cpp
if (ans <= 6) cout << ans << endl;
else cout << -1 << endl;
```

题目要求步数不超过 6，所以 `ans` 初始化为一个大哨兵值（如 114514）。如果没有任何方案使灯全亮或步数超过 6，输出 -1。

</details>

---

## 思考题

<details>
<summary>Q1 问题</summary>

为什么枚举第一行而不是第一列？如果矩阵是 n×m（n≠5），这种做法还能推广吗？

</details>

<details>
<summary>Q1 答案</summary>

因为本题是 5×5，行数固定，枚举第一行正好利用「上一行决定下一行」的性质。如果矩阵大小变化，依然可以推广：枚举第一行的 2^m 种按法，然后同样向下递推，复杂度 O(2^m · n · m)。当 m 较大时需要用状态压缩或高斯消元等方法。

</details>

<details>
<summary>Q2 问题</summary>

如果去掉「6 步以内」的限制，只要求任意步数内全亮，回溯枚举第一行的做法是否还能求出最小步数？会不会漏解？

</details>

<details>
<summary>Q2 答案</summary>

依然可以求出全局最小步数。因为第一行的按法决定了后面的所有按法，而第一行的按法只有 32 种，穷举所有第一行状态即可覆盖所有可能的「不冗余」操作序列。只要解存在，就能找到，并且步数最小。

</details>

<details>
<summary>Q3 问题</summary>

代码中为什么 `ans` 初始化为 `114514`？如果所有方案步数都大于 6，最终输出 -1 的逻辑是否正确？

</details>

<details>
<summary>Q3 答案</summary>

`114514` 是一个远大于 6 的哨兵值。如果没有任何方案能使灯全亮，`ans` 保持该值，最后 `ans <= 6` 为假，输出 -1。逻辑正确。

</details>
